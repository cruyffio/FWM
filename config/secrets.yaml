# Copy this file to secrets.yaml and fill in your keys.
# secrets.yaml is gitignored — never commit real keys.

fred:
  api_key: d133a72a6f30dd72e16dc60be22530f8   # free at https://fred.stlouisfed.org/docs/api/api_key.html

# GDELT is fully public — no key required.
# LBMA historical CSV is publicly downloadable — no key required.
# yfinance is public — no key required.
# Fed website scraping is public — no key required.
